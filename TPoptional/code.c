#include<stdio.h>
#include<stdlib.h>

char* get_next_line(int fd, int x){
	char* temp;
	
	if (read(fd, *temp,x)==-1)
		EXIT_FAILURE;
	if (read(fd, *temp,x)==0)
		printf("file is empty");
	while(read(fd, *temp,x) WWW!$£	

}
main(){
	int fd;
	fd =open("file.txt", 0_RDONLY)) // get the file descriptor 

	return 0;
}
